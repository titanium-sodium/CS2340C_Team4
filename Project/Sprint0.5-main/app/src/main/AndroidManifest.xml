<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SprintProject"
        tools:targetApi="31">

        <activity
            android:name=".views.SplashPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.CreateAccountPage" />
        <activity android:name=".views.LoginPage" />
        <activity android:name=".views.HomePage" />
        <activity android:name=".views.DestinationsPage" />
        <activity android:name=".views.DiningEstablishmentsPage" />
        <activity android:name=".views.TransportationPage" />
        <activity android:name=".views.AccommodationsPage" />
        <activity android:name=".views.LogisticsPage" />
        <activity android:name=".views.TravelCommunityPage" />

    </application>

</manifest>